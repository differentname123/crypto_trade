现在我有一个需求如下，需要一个完整的提示词来让大模型来实现这个需求，请帮我把下面的需求转换为一个完整的提示词：
1.我需要的是简单可靠的网格交易方案，我是为了在biance进行带单，所有我不能够使用现成的网格交易策略，要进行手动实现
2.整体的架构应该从高到低的设计，先思考这个系统有哪些流程，然后再一步一步的进行相应函数的实现，这样的话就能够保证低耦合，高内聚，可读性可灵活性都会提高
3.核心就是可靠和高速，而且网格看成一个个的节点，每个节点事实也是独立不影响的，然后只用管理好每个节点，整体系统就会很好，而且很稳定可靠，因为在一开始就知道目标开仓价和目标平仓价，然后再做好监控就行了，保证每个价位都有网格，做到不重复，不遗漏这就是最好的，反应要快避免错过机会
4.其实最小可运行单位就只用输入币种，以及网格间距百分比，以及每个交易数量，还有就是价格范围，方向默认是只做多，这些信息定好了，其实每个网格的数据就定好了，剩下的就是不断的监控和执行挂单操作，确保每个网格都能被正确执行。而且理论上运行中每个价格上都应该有单子，要么是开仓单要么是平仓单，而且默认是初始化建仓，也就是说假如是做多的网格，当前价格是90，上限是100，下线是50，间隔是1%，那么就应该从50到100都挂上限价单，而不只是50到90，而且就算挂95的成交价是90，但是平仓价仍然要是96
5.对账的部分应该考虑的传统的rest api可能会限速，而websocket可能又会由于波动导致订单丢失，这个对账是重中之重，这个要设计的高可靠才是一切的基础
6.我只做合约网格，然后都是双向持仓模式，而且我的资金很小，不用考虑部分成交的情况，相应网格全量成交才进行新网格的挂单操作
7.我希望交易可追溯和统计，比如我希望能够知道每次交易的时间，以及当前该币网格交易的持仓情况
8.同一个账户还可能有其它挂单，所以这个网格交易策略尽量不要影响其它人，只要保证自己网格的挂单和执行就行，井水不犯河水
9.给我相应的方案就行，暂时不用提供代码



提示词其它要求如下：
1. 为模型设计，而非为文采
    核心思想: 指令的清晰度和结构性远比语言的华丽更重要。模型是逻辑驱动的，需要的是无歧义、结构化的输入。
    行动指令: 在设计提示词时，优先提供明确的背景(Context)、角色(Role)、限制(Constraints)和输出格式(Format)，以减少模型的猜测空间，确保它能精确理解任务。
2. 以终为始，过程引导
    核心思想: 先清晰定义最终想要获得的理想输出，然后反向推导并提供所有必要的前提条件、信息和思考步骤。
    行动指令: 避免直接命令“给我结果”，而是通过提供上下文和分步指令，引导模型“像这样思考，以达成那个结果”。这本质上是为模型构建一条清晰的“思维链”（Chain of Thought）。
3.更倾向于在提示词中描述需求，尽量不要举例，不然的话会让大模型只思考举例的方向了。然后一定要避免 提示词限制思考范围，因为有时候一不注意，这个提示词就会隐性的表达出倾向，然后后续所有的结果就不一定是那么客观和期望的效果了