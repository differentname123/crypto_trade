现在我有一个需求如下，需要一个完整的提示词来让大模型来实现这个需求，请帮我把下面的需求转换为一个完整的提示词：

【背景与目标】
对于图文 内容中的图片进行降维，会对图片进行结构化描述，提取出图片的核心信息和语义价值，便于后续的检索、分析和理解。
系统的输入分为两部分：一是带有占位符的完整文章，二是对应的纯图片附件。由于附件不包含文件名，图片必须且只能通过上传的先后顺序与文章中的占位符进行对应。
联系上下文得到最终需要的字段。


字段要求如下：
    【image_type】必须从以下枚举中选择：photo(照片)、chart(图表)、screenshot(截图)、
    meme(梗图/表情包)、illustration(插画)、diagram(示意图)。不可自创。

    【visual_fact.entities】只提取画面中清晰可见的3-6个核心实体名词（人/物/地点）。
    禁止形容词，禁止抽象概念。格式：字符串数组。

    【visual_fact.description】用1-2句话白描"谁、在哪、在做什么、基础环境"。
    像盲人解说员一样陈述客观事实，禁止推测和象征。

    【visual_fact.ocr_text】提取画面中视觉显著的核心文字（海报标题、梗图文案、图表结论）。
    忽略细小背景文字。无文字则输出空字符串""。禁止编造。

    【semantic_core.concepts】结合前后文，提取2-4个抽象主题/概念标签。
    例如：前文讲加班，"打工人喝咖啡"图的概念为["职场压力","提神醒脑"]。
    前文讲精品生活，同图概念为["生活仪式感","精品咖啡"]。

    【semantic_core.message】用一句话总结"这张图在文章中真正想表达什么"。
    20-40字。禁止复述画面内容，必须提炼语义价值。

    【narrative_role.role】必须从以下枚举中选择：cover(封面)、evidence(事实论证)、
    data_chart(数据支撑)、tutorial(步骤指导)、atmosphere(氛围渲染)、meme(梗图调节)、
    decorative(装饰点缀)。

    【narrative_role.logic_bridge】结合前后文，用一句话说明：
    "这张图为什么出现在这里，它如何承接上文、引出下文"。
    禁止复制原文，必须输出逻辑因果关系。

    【narrative_role.style_tags】提取视觉风格标签数组，必须包含：
    媒介类型（如实拍/插画/3D渲染）+ 色调（如冷色调/高饱和）+ 质感（如胶片/极简）。
    格式：字符串数组，每个标签2-4字。

    【narrative_role.emotion】提取1-2个情绪形容词，描述图片传递的情绪基调。
    如："专业严谨"、"戏谑吐槽"、"温馨治愈"、"焦虑压抑"。无情绪则填"客观中立"。

每个图片必要的字段如下：
    {
      "image_id": "string",
      "image_type": "enum",
      "visual_fact": {
        "entities": [],
        "description": "",
        "ocr_text": ""
      },
      "semantic_core": {
        "concepts": [],
        "message": ""
      },
      "narrative_role": {
        "role": "",
        "logic_bridge": "",
        "style_tags": [],
        "emotion": ""
      }
    }

完整的文章的输入要预留在最后，这到时候就能够很容易拼接得到完整的提示词


提示词其它要求如下：
### 核心心智模型
**把提示词当成写给资深工程师的需求单**：已知条件写准，验收标准写死，禁区画清，交付格式说明白，然后让模型自己解题。
---
### 标准化四段式结构
在编写提示词时，强制使用明确的标题或标签（如 XML/Markdown）将内容物理隔离：
1. **`# 任务 (Task)`**：
* **做什么 / 给谁用**：明确目标与核心受众。
* **验收标准**：做到什么程度算成功（具体的指标、测试用例等，模型会据此进行内部自检）。

2. **`# 背景 (Context)`**：
* **喂足事实**：提供所有相关的客观数据、现状与已有假设。
* **材料隔离**：大块的文档/代码/数据，必须用标签（如 `<data></data>`）包裹。

3. **`# 约束 (Constraints)`**：
* **硬性边界**：预算、时间、技术限制及冲突时的优先级。
* **异常与红线**：禁止性规则直接正述（如“禁止编造，信息不足时直接提问”）。

4. **`# 输出 (Output)`**：
* **交付规范**：明确格式、结构和篇幅。
* **复核机制**：若需人工审核，要求模型附上“结论的关键依据与假设”，而非完整推理过程。
---
### 五大设计原则
1. **定问题，不定解法**：明确目标和边界，把具体的解题路径留给模型。**删除教导模型分步思考的指令**，用“验收标准”代替“过程引导”。
2. **事实做加法，指令做减法**：补充足量决策所需的背景和数据；**坚决删除**情绪激励（“深呼吸”）、增智型人设（“你是智商200的专家”）和重复强调。
3. **角色仅用于定位视角**：人设不用于提升智商，只用于限定输出视角或受众语境（如：“从安全审计视角审查”或“面向 CTO 的决策报告”）。
4. **零样本优先，示例仅限格式**：优先通过清晰的需求描述来约束输出。若必须提供示例，**仅提供输入-输出对**，并强制声明：“*仅为格式示例，勿套用其中逻辑*”，防止模型思维窄化。
5. **确保约束无矛盾**：发布前自查规则逻辑。现代推理模型会死抠字眼，矛盾的指令会浪费算力去猜测你的真实意图。